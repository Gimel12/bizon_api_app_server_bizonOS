{
  "name": "bizon-ssh-api",
  "version": "2.0.0",
  "description": "Bizon-Tech Workstation API Server + MCP Server",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "mcp": "node mcp-server.js --transport stdio",
    "mcp:sse": "node mcp-server.js --transport sse --port 4001",
    "start:all": "node index.js & node mcp-server.js --transport sse --port 4001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "@anthropic-ai/sdk": "^0.39.0",
    "ssh2": "^1.15.0",
    "zod": "^3.22.0"
  }
}
